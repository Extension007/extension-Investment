# 📊 ОТЧЕТ: Аудит различий между локальной средой и продакшеном Vercel

**Дата:** 2025-01-27  
**Локальная среда:** `http://localhost:3000`  
**Продакшен:** `https://extension-investment.vercel.app`  
**Статус:** Анализ без изменений кода

---

## 📋 СОДЕРЖАНИЕ

1. [Проверка параметра origin](#1-проверка-параметра-origin)
2. [Проверка CSP (Content-Security-Policy)](#2-проверка-csp-content-security-policy)
3. [Проверка окружения и переменных](#3-проверка-окружения-и-переменных)
4. [Проверка шаблонов на дублирование](#4-проверка-шаблонов-на-дублирование)
5. [Проверка стилей](#5-проверка-стилей)
6. [Выводы и рекомендации](#выводы-и-рекомендации)

---

## 1. ПРОВЕРКА ПАРАМЕТРА ORIGIN

### 1.1. Анализ кода

**Файл:** `public/script.js`

#### ✅ В основной реализации (YT.Player API):

**Строка 252:**
```javascript
const origin = encodeURIComponent(window.location.origin);
```

**Строка 265:**
```javascript
playerVars: {
  'origin': window.location.origin, // Домен для безопасности
  // ...
}
```

#### ✅ В fallback функции:

**Строка 401:**
```javascript
const origin = encodeURIComponent(window.location.origin);
const embedUrl = `https://www.youtube.com/embed/${videoId}?playsinline=1&controls=1&rel=0&enablejsapi=1&origin=${origin}`;
```

**Вывод:** ✅ **Параметр origin корректно использует `window.location.origin`**

### 1.2. Поведение на разных средах

**Локальная среда (localhost:3000):**
- `window.location.origin` = `"http://localhost:3000"`
- `origin` в playerVars = `"http://localhost:3000"`
- ✅ YouTube принимает этот origin для локальной разработки

**Продакшен (Vercel):**
- `window.location.origin` = `"https://extension-investment.vercel.app"`
- `origin` в playerVars = `"https://extension-investment.vercel.app"`
- ✅ YouTube должен принимать этот origin

### 1.3. Проверка на хардкод localhost

**Результат поиска:**
```
grep -i "localhost|127.0.0.1|3000" public/script.js
→ Нет совпадений
```

**Вывод:** ✅ **Нет хардкода localhost в JavaScript коде**

**Найдено в server.js:**
- Только в логе запуска: `console.log('✅ Сервер запущен на http://localhost:${port}');`
- Это не влияет на работу видео-плееров

### 1.4. Итоговая оценка

| Параметр | Статус | Комментарий |
|----------|--------|-------------|
| Использование `window.location.origin` | ✅ | Используется корректно везде |
| Отсутствие хардкода localhost | ✅ | Хардкода нет |
| Корректность на продакшене | ✅ | Будет `https://extension-investment.vercel.app` |

**Вывод:** ✅ **Параметр origin настроен корректно. На продакшене будет автоматически использоваться правильный домен.**

---

## 2. ПРОВЕРКА CSP (CONTENT-SECURITY-POLICY)

### 2.1. Анализ настроек CSP в server.js

**Файл:** `server.js` (строки 96-113)

**Текущие настройки:**
```javascript
app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: [
        "'self'", 
        "'unsafe-inline'", 
        "https://www.youtube.com", 
        "https://youtube.com", 
        "https://*.youtube.com"
      ],
      styleSrc: ["'self'", "'unsafe-inline'", "https:"],
      fontSrc: ["'self'", "https:", "data:"],
      imgSrc: ["'self'", "data:", "https:", "blob:", "https://res.cloudinary.com"],
      connectSrc: [
        "'self'", 
        "https:", 
        "https://api.instagram.com"
      ],
      frameSrc: [
        "'self'",
        "https://www.youtube.com",
        "https://youtube.com",
        "https://youtu.be",
        "https://*.youtube.com",
        "https://www.youtube-nocookie.com",
        "https://m.youtube.com",
        "https://vk.com",
        "https://*.vk.com",
        "https://www.instagram.com",
        "https://*.instagram.com"
      ],
      mediaSrc: ["'self'", "https:"],
      objectSrc: ["'none'"]
    }
  },
  crossOriginEmbedderPolicy: false,
  crossOriginResourcePolicy: { policy: "cross-origin" }
}));
```

### 2.2. Проверка необходимых разрешений

#### ✅ YouTube IFrame API (scriptSrc):

**Требуется:**
- `https://www.youtube.com` ✅ Присутствует
- `https://*.youtube.com` ✅ Присутствует (покрывает поддомены)

**Вывод:** ✅ **YouTube IFrame API может загружаться**

#### ✅ YouTube iframe (frameSrc):

**Требуется:**
- `https://www.youtube.com` ✅ Присутствует
- `https://*.youtube.com` ✅ Присутствует
- `https://www.youtube-nocookie.com` ✅ Присутствует (для приватности)

**Вывод:** ✅ **YouTube iframe может загружаться**

#### ✅ VK iframe (frameSrc):

**Требуется:**
- `https://vk.com` ✅ Присутствует
- `https://*.vk.com` ✅ Присутствует (покрывает поддомены)

**Вывод:** ✅ **VK iframe может загружаться**

#### ✅ Instagram iframe (frameSrc):

**Требуется:**
- `https://www.instagram.com` ✅ Присутствует
- `https://*.instagram.com` ✅ Присутствует (покрывает поддомены)

**Вывод:** ✅ **Instagram iframe может загружаться**

#### ⚠️ Instagram oEmbed API (connectSrc):

**Требуется:**
- `https://api.instagram.com` ✅ Присутствует

**Вывод:** ✅ **Instagram oEmbed API запросы разрешены**

#### ❓ Facebook Graph API (для Instagram):

**Текущее состояние:** ❌ **Отсутствует**

**Требуется (если используется Facebook Graph API для Instagram):**
- `https://graph.facebook.com` ❌ **НЕ присутствует в CSP**

**Комментарий:** 
- В текущем коде Instagram использует публичный oEmbed API (`https://api.instagram.com/oembed`)
- Facebook Graph API не используется в текущей реализации
- Если в будущем потребуется Facebook Graph API, нужно добавить `https://graph.facebook.com` в `connectSrc`

### 2.3. Проверка на Vercel

**Важно:** Vercel может добавлять дополнительные CSP заголовки или перезаписывать существующие.

**Рекомендуемая проверка:**

1. Открыть DevTools в браузере
2. Перейти на вкладку Network
3. Загрузить страницу на продакшене
4. Проверить заголовок `Content-Security-Policy` в ответе
5. Убедиться, что все необходимые домены присутствуют

**Ожидаемый результат на Vercel:**
```
Content-Security-Policy: 
  default-src 'self';
  script-src 'self' 'unsafe-inline' https://www.youtube.com https://youtube.com https://*.youtube.com;
  frame-src 'self' https://www.youtube.com https://youtube.com https://*.youtube.com https://vk.com https://*.vk.com https://www.instagram.com https://*.instagram.com;
  connect-src 'self' https: https://api.instagram.com;
  ...
```

### 2.4. Итоговая оценка

| Директива | Статус | Комментарий |
|-----------|--------|-------------|
| `scriptSrc` для YouTube API | ✅ | Все необходимые домены присутствуют |
| `frameSrc` для YouTube | ✅ | Все необходимые домены присутствуют |
| `frameSrc` для VK | ✅ | Все необходимые домены присутствуют |
| `frameSrc` для Instagram | ✅ | Все необходимые домены присутствуют |
| `connectSrc` для Instagram API | ✅ | `https://api.instagram.com` присутствует |
| `connectSrc` для Facebook Graph API | ⚠️ | Не требуется в текущей реализации |

**Вывод:** ✅ **CSP настроен корректно. Все необходимые разрешения присутствуют. Facebook Graph API не требуется, так как используется публичный Instagram oEmbed API.**

**⚠️ Рекомендация:** Проверить фактические CSP заголовки на Vercel, так как они могут отличаться от настроек в коде.

---

## 3. ПРОВЕРКА ОКРУЖЕНИЯ И ПЕРЕМЕННЫХ

### 3.1. Анализ Instagram oEmbed API

**Файл:** `server.js` (строки 1488-1548)

**Текущая реализация:**
```javascript
app.get("/api/instagram/oembed", async (req, res) => {
  // ...
  const oembedUrl = `https://api.instagram.com/oembed?url=${encodeURIComponent(url)}`;
  // ...
});
```

**Вывод:** ✅ **Instagram oEmbed API - публичный API, не требует access_token**

**Документация Instagram oEmbed API:**
- Публичный endpoint: `https://api.instagram.com/oembed?url={instagram_url}`
- Не требует авторизации
- Не требует access_token
- Работает без переменных окружения

### 3.2. Проверка переменных окружения

**Необходимые переменные для видео-плееров:**

| Переменная | Назначение | Обязательна | Статус |
|------------|------------|-------------|--------|
| `INSTAGRAM_ACCESS_TOKEN` | Instagram API токен | ❌ НЕТ | Не используется (публичный oEmbed API) |
| `FACEBOOK_ACCESS_TOKEN` | Facebook Graph API токен | ❌ НЕТ | Не используется |
| `MONGODB_URI` | MongoDB подключение | ⚠️ Да (для сессий) | Для сессий, не влияет на видео |
| `SESSION_SECRET` | Секрет для сессий | ⚠️ Да (рекомендуется) | Для сессий, не влияет на видео |

**Вывод:** ✅ **Для работы видео-плееров не требуется настройка переменных окружения**

### 3.3. Проверка runtime доступности

**Локальная среда:**
- Переменные окружения загружаются из `.env` через `dotenv`
- ✅ Работает корректно

**Продакшен (Vercel):**
- Переменные окружения настраиваются в Vercel Dashboard → Settings → Environment Variables
- ✅ Если переменные не настроены, код работает с fallback значениями

**Проверка в коде:**

**Файл:** `server.js` (строка 129):
```javascript
secret: process.env.SESSION_SECRET || "exto-secret",
```

**Вывод:** ✅ **Код использует fallback значения, если переменные не настроены**

### 3.4. Итоговая оценка

| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| Instagram API токен | ✅ | Не требуется (публичный API) |
| Facebook API токен | ✅ | Не требуется |
| Переменные окружения | ✅ | Не требуются для видео-плееров |
| Runtime доступность | ✅ | Код работает с fallback значениями |

**Вывод:** ✅ **Окружение настроено корректно. Для работы видео-плееров не требуется настройка переменных окружения.**

---

## 4. ПРОВЕРКА ШАБЛОНОВ НА ДУБЛИРОВАНИЕ

### 4.1. Анализ шаблонов

#### ✅ `views/index.ejs` - ОСНОВНАЯ РЕАЛИЗАЦИЯ

**Статус:** ✅ **Корректно использует единую логику из `public/script.js`**

**Проверка:**
- ✅ Загружает YouTube IFrame API скрипт
- ✅ Подключает `public/script.js`
- ✅ Использует функции из `public/script.js`
- ✅ Нет дублирующего inline-кода для создания iframe

#### ❌ `views/admin.ejs` - ДУБЛИРОВАНИЕ КОДА

**Файл:** `views/admin.ejs` (строки 510-557)

**Проблема:** ⚠️ **Есть дублирующий inline-скрипт с упрощенной логикой**

**Найденный код:**
```javascript
function createYouTubeIframe(videoId) {
  // ...
  const origin = encodeURIComponent(window.location.origin);
  const embedUrl = `https://www.youtube.com/embed/${videoId}?playsinline=1&controls=1&rel=0&origin=${origin}`;
  
  const iframe = document.createElement('iframe');
  // ... создание простого iframe БЕЗ YouTube IFrame API
}
```

**Проблемы:**
- ❌ Использует простой iframe **БЕЗ** YouTube IFrame API
- ❌ Отсутствует параметр `enablejsapi=1` (критично для исправления ошибки 153)
- ❌ Нет обработки событий `onReady`, `onError`, `onStateChange`
- ❌ Нет обработки ошибки 153
- ❌ Нет защиты от двойного вызова `play()`

**Вывод:** 🔴 **КРИТИЧЕСКАЯ ПРОБЛЕМА - дублирование кода без YouTube IFrame API**

#### ❌ `views/cabinet.ejs` - ДУБЛИРОВАНИЕ КОДА

**Файл:** `views/cabinet.ejs` (строки 369-416)

**Проблема:** ⚠️ **Есть дублирующий inline-скрипт с упрощенной логикой**

**Найденный код:**
```javascript
function createYouTubeIframe(videoId) {
  // ... идентичный код как в admin.ejs
}
```

**Проблемы:**
- ❌ Те же проблемы, что и в `admin.ejs`

**Вывод:** 🔴 **КРИТИЧЕСКАЯ ПРОБЛЕМА - дублирование кода без YouTube IFrame API**

### 4.2. Проверка подключения script.js

**admin.ejs:**
- ❓ Не проверено подключение `public/script.js`
- ❓ Возможно, используется только inline-скрипт

**cabinet.ejs:**
- ❓ Не проверено подключение `public/script.js`
- ❓ Возможно, используется только inline-скрипт

### 4.3. Сравнение реализаций

| Файл | YouTube IFrame API | enablejsapi=1 | Обработка ошибок | Статус |
|------|-------------------|---------------|------------------|--------|
| `public/script.js` | ✅ Да | ✅ Да | ✅ Да | ✅ Правильная реализация |
| `views/index.ejs` | ✅ Да | ✅ Да | ✅ Да | ✅ Использует script.js |
| `views/admin.ejs` | ❌ Нет | ❌ Нет | ❌ Нет | 🔴 Дублирование |
| `views/cabinet.ejs` | ❌ Нет | ❌ Нет | ❌ Нет | 🔴 Дублирование |

### 4.4. Итоговая оценка

| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| Единая логика в `public/script.js` | ✅ | Реализация корректна |
| Использование в `index.ejs` | ✅ | Используется корректно |
| Дублирование в `admin.ejs` | 🔴 | Есть упрощенная реализация без API |
| Дублирование в `cabinet.ejs` | 🔴 | Есть упрощенная реализация без API |

**Вывод:** 🔴 **КРИТИЧЕСКАЯ ПРОБЛЕМА - в `admin.ejs` и `cabinet.ejs` есть дублирующий код, который использует простой iframe без YouTube IFrame API. Это может привести к ошибке 153 и проблемам с воспроизведением на продакшене.**

**⚠️ Рекомендация:** Удалить дублирующий код из `admin.ejs` и `cabinet.ejs`, убедиться, что подключен `public/script.js`, и использовать единую логику.

---

## 5. ПРОВЕРКА СТИЛЕЙ

### 5.1. Анализ адаптивности контейнеров

**Файл:** `public/styles.css` (строки 612-680)

#### ✅ Overlay контейнер

```css
.video-overlay {
  position: fixed;
  inset: 0;
  width: 100%;
  height: 100%;
  padding: 20px;
  box-sizing: border-box;
}
```

**Вывод:** ✅ **Overlay адаптивен (width: 100%, height: 100%)**

#### ✅ Видео контейнер (резиновый контейнер)

```css
.video-overlay-container {
  position: relative;
  width: 100%;
  max-width: 960px;
  padding-bottom: 56.25%; /* 16:9 aspect ratio */
  height: 0;
  min-height: 0;
  max-height: 90vh;
}
```

**Вывод:** ✅ **Используется резиновый контейнер (padding-bottom), адаптивен**

#### ✅ Iframe контейнер

```css
.video-overlay-iframe-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
```

**Вывод:** ✅ **Iframe контейнер адаптивен (width: 100%, height: 100%)**

#### ✅ Iframe элемент

```css
.video-overlay-iframe-container iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
```

**Вывод:** ✅ **Iframe адаптивен (width: 100%, height: 100%)**

### 5.2. Проверка фиксированных размеров

**Поиск фиксированных размеров:**
```
grep "width.*px|height.*px" public/styles.css | grep -i video
```

**Найденные фиксированные размеры:**
- `max-width: 960px` - ✅ Ограничение максимальной ширины (правильно)
- `max-height: 90vh` - ✅ Ограничение максимальной высоты (правильно)
- `min-height: 600px` для Instagram - ✅ Минимальная высота для Instagram (правильно)

**Вывод:** ✅ **Нет проблемных фиксированных размеров. Все размеры адаптивны или являются разумными ограничениями.**

### 5.3. Проверка aspect-ratio

**Использование aspect-ratio:**
```css
.video-overlay-container {
  padding-bottom: 56.25%; /* 16:9 aspect ratio */
}
```

**Вывод:** ✅ **Используется техника резинового контейнера с `padding-bottom`, которая не ломается при минификации CSS**

**Альтернатива (более современная):**
```css
aspect-ratio: 16 / 9;
```
Но текущий подход с `padding-bottom` более совместим и не ломается при минификации.

### 5.4. Проверка минификации CSS на Vercel

**Возможные проблемы:**
- Vercel может минифицировать CSS
- Минификация не должна ломать `padding-bottom: 56.25%`

**Вывод:** ✅ **Техника резинового контейнера с `padding-bottom` безопасна для минификации**

### 5.5. Медиа-запросы для ориентации

**Добавлены медиа-запросы:**
- `@media (orientation: portrait)` ✅
- `@media (orientation: landscape)` ✅
- Комбинированные медиа-запросы ✅

**Вывод:** ✅ **Медиа-запросы для ориентации присутствуют и корректны**

### 5.6. Итоговая оценка

| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| Адаптивность overlay | ✅ | width: 100%, height: 100% |
| Адаптивность контейнеров | ✅ | Используется резиновый контейнер |
| Адаптивность iframe | ✅ | width: 100%, height: 100% |
| Отсутствие фиксированных размеров | ✅ | Нет проблемных фиксированных размеров |
| Безопасность aspect-ratio | ✅ | Используется padding-bottom (безопасно для минификации) |
| Медиа-запросы для ориентации | ✅ | Присутствуют и корректны |

**Вывод:** ✅ **Стили настроены корректно. Все контейнеры и overlay адаптивны. Минификация CSS на Vercel не должна вызывать проблем.**

---

## 📊 ВЫВОДЫ И РЕКОМЕНДАЦИИ

### ✅ Что работает корректно

1. **Параметр origin:**
   - ✅ Используется `window.location.origin` везде
   - ✅ Нет хардкода localhost
   - ✅ На продакшене будет автоматически `https://extension-investment.vercel.app`

2. **CSP настройки:**
   - ✅ Все необходимые домены разрешены
   - ✅ YouTube, VK, Instagram iframe могут загружаться
   - ✅ YouTube IFrame API может загружаться

3. **Переменные окружения:**
   - ✅ Не требуются для работы видео-плееров
   - ✅ Instagram oEmbed API - публичный API
   - ✅ Код использует fallback значения

4. **Стили:**
   - ✅ Все контейнеры адаптивны
   - ✅ Используется резиновый контейнер
   - ✅ Медиа-запросы для ориентации присутствуют

### 🔴 Критические проблемы

1. **Дублирование кода в `admin.ejs` и `cabinet.ejs`:**
   - 🔴 Используется простой iframe **БЕЗ** YouTube IFrame API
   - 🔴 Отсутствует параметр `enablejsapi=1`
   - 🔴 Нет обработки ошибок (включая ошибку 153)
   - 🔴 Нет защиты от двойного вызова `play()`

**Влияние:**
- ⚠️ В админ-панели и личном кабинете видео могут не работать корректно
- ⚠️ Ошибка 153 может возникать
- ⚠️ Кнопка Play может не отображаться
- ⚠️ Видео может не воспроизводиться

### ⚠️ Рекомендации

#### 1. Немедленно исправить:

1. **Удалить дублирующий код из `admin.ejs`:**
   - Удалить inline-скрипт с функцией `createYouTubeIframe`
   - Убедиться, что подключен `public/script.js`
   - Использовать единую логику из `public/script.js`

2. **Удалить дублирующий код из `cabinet.ejs`:**
   - Удалить inline-скрипт с функцией `createYouTubeIframe`
   - Убедиться, что подключен `public/script.js`
   - Использовать единую логику из `public/script.js`

3. **Добавить загрузку YouTube IFrame API в `admin.ejs` и `cabinet.ejs`:**
   ```html
   <!-- Загружаем YouTube IFrame API перед основным скриптом -->
   <script>
     var tag = document.createElement('script');
     tag.src = "https://www.youtube.com/iframe_api";
     var firstScriptTag = document.getElementsByTagName('script')[0];
     firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);
   </script>
   ```

#### 2. Проверить на продакшене:

1. **Проверить CSP заголовки:**
   - Открыть DevTools → Network
   - Проверить заголовок `Content-Security-Policy`
   - Убедиться, что все необходимые домены присутствуют

2. **Проверить работу видео:**
   - YouTube обычные видео
   - YouTube Shorts
   - VK видео
   - Instagram Reels

3. **Проверить консоль браузера:**
   - Открыть DevTools → Console
   - Проверить наличие ошибок
   - Проверить логи загрузки YouTube IFrame API

#### 3. Опционально (если потребуется):

1. **Добавить Facebook Graph API в CSP:**
   - Если в будущем потребуется Facebook Graph API для Instagram
   - Добавить `https://graph.facebook.com` в `connectSrc`

---

## 📝 ИТОГОВАЯ ТАБЛИЦА ПРОВЕРОК

| Проверка | Статус | Комментарий |
|----------|--------|-------------|
| **1. Параметр origin** | ✅ | Корректно использует `window.location.origin` |
| **2. Отсутствие хардкода localhost** | ✅ | Хардкода нет |
| **3. CSP для YouTube API** | ✅ | Все необходимые домены разрешены |
| **4. CSP для YouTube iframe** | ✅ | Все необходимые домены разрешены |
| **5. CSP для VK iframe** | ✅ | Все необходимые домены разрешены |
| **6. CSP для Instagram iframe** | ✅ | Все необходимые домены разрешены |
| **7. CSP для Instagram API** | ✅ | `https://api.instagram.com` разрешен |
| **8. Переменные окружения** | ✅ | Не требуются для видео-плееров |
| **9. Дублирование в `index.ejs`** | ✅ | Нет дублирования |
| **10. Дублирование в `admin.ejs`** | 🔴 | **Есть дублирование без API** |
| **11. Дублирование в `cabinet.ejs`** | 🔴 | **Есть дублирование без API** |
| **12. Адаптивность overlay** | ✅ | width: 100%, height: 100% |
| **13. Адаптивность контейнеров** | ✅ | Резиновый контейнер |
| **14. Безопасность aspect-ratio** | ✅ | Используется padding-bottom |

---

## 🎯 ЗАКЛЮЧЕНИЕ

**Основная причина проблемы на продакшене:** 🔴 **Дублирование кода в `admin.ejs` и `cabinet.ejs`**

**Что нужно исправить:**
1. Удалить дублирующий код из `admin.ejs` и `cabinet.ejs`
2. Добавить загрузку YouTube IFrame API в эти шаблоны
3. Убедиться, что подключен `public/script.js`

**Остальные аспекты работают корректно:**
- ✅ Параметр origin настроен правильно
- ✅ CSP настроен правильно
- ✅ Переменные окружения не требуются
- ✅ Стили адаптивны

**После исправления дублирования кода видео-плееры должны работать корректно на продакшене.**

---

**Отчет подготовлен:** 2025-01-27  
**Версия:** 1.0  
**Статус:** Готов к использованию

