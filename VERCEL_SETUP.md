# Настройка проекта для Vercel

## ⚠️ Важно: Vercel - это serverless окружение

В serverless окружениях (Vercel, AWS Lambda) файловая система доступна **только для чтения**. 
Поэтому **локальное хранилище файлов не работает** - необходимо использовать **Cloudinary**.

## 🔧 Настройка Cloudinary на Vercel

### 1. Получите учетные данные Cloudinary

1. Зарегистрируйтесь на [cloudinary.com](https://cloudinary.com)
2. Перейдите в Dashboard
3. Скопируйте:
   - **Cloud Name**
   - **API Key**
   - **API Secret**

### 2. Добавьте переменные окружения в Vercel

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

```
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

4. **Важно:** Выберите все окружения (Production, Preview, Development)
5. Нажмите **Save**

### 3. Перезапустите деплой

После добавления переменных окружения:
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (или сделайте новый коммит)

## ✅ Проверка

После деплоя проверьте логи Vercel. Должно быть:
```
✅ Cloudinary настроен
```

Если видите ошибку:
```
❌ В serverless окружении (Vercel) Cloudinary обязателен!
```

Это значит, что переменные окружения не настроены или не применены. Проверьте:
1. Правильность имен переменных (должны быть точно как указано выше)
2. Что переменные добавлены для всех окружений
3. Что деплой был перезапущен после добавления переменных

## 📋 Другие необходимые переменные окружения

Для полной работы проекта также нужны:

```
MONGODB_URI=your_mongodb_connection_string
SESSION_SECRET=your_random_secret_string
PORT=3000 (опционально)
```

## 🔍 Отладка

Если проблемы сохраняются:

1. **Проверьте логи Vercel:**
   - Откройте Deployment → Logs
   - Ищите сообщения об ошибках

2. **Проверьте переменные окружения:**
   - Settings → Environment Variables
   - Убедитесь, что все переменные добавлены

3. **Проверьте формат переменных:**
   - Не должно быть пробелов в начале/конце
   - Имена переменных должны быть точно как указано

## 📝 Примечания

- В локальной разработке можно использовать локальное хранилище (если Cloudinary не настроен)
- В production на Vercel **обязательно** нужен Cloudinary
- Локальные файлы в папке `uploads/` не будут работать на Vercel





