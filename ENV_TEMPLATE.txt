# ============================================
# КРИТИЧНЫЕ ПЕРЕМЕННЫЕ (обязательны для production)
# ============================================

# Секретный ключ для сессий (минимум 32 символа)
# Сгенерировать: openssl rand -base64 32
# ❌ НЕ используйте дефолтное значение "exto-secret"
SESSION_SECRET=REPLACE_WITH_GENERATED_SECRET_MIN_32_CHARS

# MongoDB Atlas connection string
# Формат: mongodb+srv://username:password@cluster.mongodb.net/exto?retryWrites=true&w=majority
# Получить: https://cloud.mongodb.com/ → Database → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/exto

# ============================================
# ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ (рекомендуется для production)
# ============================================

# Cloudinary (для хранения изображений в облаке)
# Получить: https://cloudinary.com/ → Settings → Access Keys
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Redis (для кеширования)
REDIS_URL=

# Sentry (для отслеживания ошибок)
SENTRY_DSN=

# ============================================
# НАСТРОЙКИ ОКРУЖЕНИЯ
# ============================================

# Окружение (development | production)
NODE_ENV=development

# Порт сервера (по умолчанию 3000)
PORT=3000

# ============================================
# ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ
# ============================================

# 1. Скопируйте этот файл в .env:
#    cp ENV_TEMPLATE.txt .env

# 2. Сгенерируйте SESSION_SECRET:
#    openssl rand -base64 32

# 3. Заполните MONGODB_URI из MongoDB Atlas

# 4. Запустите сервер:
#    npm start















