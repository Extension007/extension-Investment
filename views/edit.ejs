<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Редактирование товара</title>
</head>
<body>
  <h1>Редактирование товара</h1>

  <form method="POST" action="/admin/product/<%= product._id %>/edit" enctype="multipart/form-data">
    <input type="text" name="name" value="<%= product.name %>" placeholder="Название" required>
    <input type="text" name="description" value="<%= product.description %>" placeholder="Описание">
    <input type="number" name="price" value="<%= product.price %>" placeholder="Цена" required>
    <input type="url" name="link" value="<%= product.link %>" placeholder="Ссылка" required>

    <!-- ✅ Новое поле для ссылки на YouTube видео -->
    <p>Ссылка на YouTube видео:</p>
    <input type="url" name="video_url" value="<%= product.video_url %>" placeholder="Ссылка на YouTube видео">

    <% if (product.image_url) { %>
      <div>
        <p>Текущее изображение:</p>
        <img src="<%= product.image_url %>" alt="<%= product.name %>" style="max-width: 160px; height: auto;">
      </div>
      <input type="hidden" name="current_image" value="<%= product.image_url %>">
    <% } %>

    <p>Загрузить новое изображение (необязательно):</p>
    <input type="file" name="image" accept="image/*">

    <button type="submit">Сохранить изменения</button>
  </form>

  <!-- 🔹 Блок рейтинга для редактируемого товара -->
  <h2>Рейтинг товара</h2>
  <div class="product-rating" data-id="<%= product._id %>">
    <button class="like-btn" type="button" aria-label="Лайк">👍</button>

    <span class="result">
      <% const likes = product.likes || 0; %>
      <% const dislikes = product.dislikes || 0; %>
      <%= likes - dislikes %>
    </span>

    <button class="dislike-btn" type="button" aria-label="Дизлайк">👎</button>

    <span class="votes">
      (<%= (product.likes || 0) + (product.dislikes || 0) %> голосов)
    </span>
  </div>

  <p><a href="/admin">Назад к админке</a></p>
</body>
</html>
