<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Статус подтверждения - Albamount</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.6.0/axios.min.js"></script>
</head>
<body>
  <div id="bg-animated" aria-hidden="true"></div>
  <header class="main-header">
    <div class="container header-inner">
      <a class="logo" href="/"><img src="/albamount.png" alt="ALBAMOUNT" class="logo-img"></a>
      <div class="header-actions">
        <a class="btn outline" href="/">На главную</a>
      </div>
    </div>
  </header>

  <main>
    <div class="container verification-page">
      <h1 class="page-title">Статус подтверждения email</h1>
      
      <div class="profile-content">
        <div class="profile-card">
          <h2>Информация о профиле</h2>
          
          <div class="profile-info">
            <p><strong>Имя пользователя:</strong> <%= typeof user !== 'undefined' ? user.username : 'Гость' %></p>
            <p><strong>Email:</strong> <%= typeof user !== 'undefined' ? user.email : 'Не указан' %></p>
            
            <div class="verification-status">
              <% if (typeof user !== 'undefined' && user.emailVerified) { %>
                <span class="status-badge verified">✓ Email подтвержден</span>
              <% } else { %>
                <span class="status-badge unverified">⚠ Email не подтвержден</span>
                
                <div class="verification-actions">
                  <button id="resendVerificationBtn" class="btn outline">Отправить письмо подтверждения</button>
                  <div id="resendStatus"></div>
                </div>
              <% } %>
            </div>
          
          <div class="actions">
            <a href="/cabinet" class="btn primary">Вернуться в кабинет</a>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="mobile-tabs-footer">
    <div class="mobile-tabs-container">
      <a href="/products" class="mobile-tab-button">Товары</a>
      <a href="/services" class="mobile-tab-button">Услуги</a>
      <a href="/videos" class="mobile-tab-button">Видео</a>
      <a href="/ad" class="mobile-tab-button">Реклама</a>
      <a href="/about" class="mobile-tab-button">О Нас</a>
      <a href="/contacts" class="mobile-tab-button">Контакты</a>
    </div>
  </footer>

  <script>
    document.getElementById('resendVerificationBtn')?.addEventListener('click', async function() {
      try {
        const response = await axios.post('/resend-verification', {
          email: "<%= typeof user !== 'undefined' ? user.email : '' %>",
          _csrf: '<%= typeof csrfToken !== "undefined" ? csrfToken : "" %>'
        });
        
        const statusDiv = document.getElementById('resendStatus');
        if (response.data.success) {
          statusDiv.innerHTML = '<div class="success-message">Письмо с подтверждением отправлено!</div>';
        } else {
          statusDiv.innerHTML = '<div class="error-message">' + response.data.message + '</div>';
        }
      } catch (error) {
        const statusDiv = document.getElementById('resendStatus');
        const errorMessage = error.response?.data?.message || 'Ошибка при отправке письма';
        statusDiv.innerHTML = '<div class="error-message">' + errorMessage + '</div>';
      }
    });
  </script>
</body>
</html>
