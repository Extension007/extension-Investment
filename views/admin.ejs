<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Админка — Управление товарами</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="main-header">
    <div class="container header-inner">
      <a class="logo" href="/">EXTO</a>
      <nav class="nav">
        <ul class="nav-list">
          <li><a class="nav-link" href="/">Каталог</a></li>
        </ul>
      </nav>
      <div class="header-actions">
        <span class="user-chip">Админ-панель</span>
      </div>
    </div>
  </header>

  <main class="container">
    <!-- Секция модерации -->
    <section class="section">
      <div class="section-header">
        <h2>Модерация карточек</h2>
        <span class="badge" style="background:#ff9800;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
          <% if (pendingProducts && pendingProducts.length) { %>
            <%= pendingProducts.length %> на проверке
          <% } else { %>
            Нет карточек на модерации
          <% } %>
        </span>
      </div>

      <% if (pendingProducts && pendingProducts.length) { %>
        <div class="product-grid">
          <% pendingProducts.forEach(p => { %>
            <div class="product-card" style="border:2px solid #ff9800;">
              <h3 class="product-title"><%= p.name %></h3>
              <% if (p.image_url) { %>
                <img src="<%= p.image_url %>" alt="<%= p.name %>">
              <% } else { %>
                <div class="no-image">Нет изображения</div>
              <% } %>

              <div class="product-bottom">
                <p class="product-desc"><%= p.description || "—" %></p>
                <div class="row" style="justify-content:center; gap:8px;">
                  <span class="muted">Цена:</span>
                  <strong class="price"><%= p.price %></strong>
                </div>
                <% if (p.owner) { %>
                  <% if (typeof p.owner === 'object' && p.owner.username) { %>
                    <p class="muted" style="text-align:center;font-size:0.9rem;">От: <%= p.owner.username %></p>
                  <% } else { %>
                    <p class="muted" style="text-align:center;font-size:0.9rem;">От: пользователь ID <%= p.owner %></p>
                  <% } %>
                <% } else { %>
                  <p class="muted" style="text-align:center;font-size:0.9rem;color:#f44336;">⚠️ Владелец не указан</p>
                <% } %>

                <div class="product-actions" style="flex-direction:column;gap:8px;">
                  <button class="btn small approve-btn" data-id="<%= p._id %>" style="background:#4caf50;color:#fff;">
                    ✅ Одобрить
                  </button>
                  <button class="btn small reject-btn" data-id="<%= p._id %>" style="background:#f44336;color:#fff;">
                    ❌ Отклонить
                  </button>
                  <% if (p.link) { %>
                    <a class="btn small outline" href="<%= p.link %>" target="_blank" rel="noopener">Ссылка</a>
                  <% } %>
                  <% if (p.video_url) { %>
                    <button class="btn small outline" data-video="<%= p.video_url %>">Видео</button>
                  <% } %>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="empty-state">Нет карточек на модерации</p>
      <% } %>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Добавить товар</h2>
      </div>
      <form method="POST" action="/admin/product" enctype="multipart/form-data" class="form-grid">
        <input type="text" name="name" placeholder="Название" required>
        <input type="text" name="description" placeholder="Описание">
        <input type="number" name="price" placeholder="Цена" min="0" step="1" required>
        <input type="url" name="link" placeholder="Ссылка" required>
        <input type="file" name="image" accept="image/*">
        <input type="url" name="video_url" placeholder="Ссылка на YouTube видео">
        <button type="submit" class="btn primary">Добавить</button>
      </form>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Все карточки</h2>
        <span class="badge" style="background:#666;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
          Всего: <%= products ? products.length : 0 %>
        </span>
      </div>

      <% if (products && products.length) { %>
        <div class="product-grid">
          <% products.forEach(p => { %>
            <% 
              let cardStyle = '';
              if (p.status === 'rejected') {
                cardStyle = 'opacity:0.7;border:2px solid #f44336;';
              } else if (p.status === 'approved') {
                cardStyle = 'border:2px solid #4caf50;';
              } else {
                cardStyle = 'border:2px solid #ff9800;';
              }
            %>
            <div class="product-card" style="<%= cardStyle %>">
              <h3 class="product-title"><%= p.name %></h3>
              <% if (p.image_url) { %>
                <img src="<%= p.image_url %>" alt="<%= p.name %>">
              <% } else { %>
                <div class="no-image">Нет изображения</div>
              <% } %>

              <div class="product-bottom">
                <!-- Статус карточки -->
                <div style="text-align:center;margin-bottom:10px;">
                  <% if (p.status === "pending") { %>
                    <span class="badge" style="background:#ff9800;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ⏳ На модерации
                    </span>
                  <% } else if (p.status === "approved") { %>
                    <span class="badge" style="background:#4caf50;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ✅ Опубликовано
                    </span>
                  <% } else if (p.status === "rejected") { %>
                    <span class="badge" style="background:#f44336;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ❌ Заблокировано
                    </span>
                  <% } else { %>
                    <span class="badge" style="background:#666;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ⚠️ Без статуса
                    </span>
                  <% } %>
                </div>

                <p class="product-desc"><%= p.description || "—" %></p>
                <div class="row" style="justify-content:center; gap:8px;">
                  <span class="muted">Цена:</span>
                  <strong class="price"><%= p.price %></strong>
                </div>

                <!-- Владелец -->
                <% if (p.owner) { %>
                  <% if (typeof p.owner === 'object' && p.owner.username) { %>
                    <p class="muted" style="text-align:center;font-size:0.85rem;margin-top:5px;">
                      👤 Владелец: <%= p.owner.username %>
                    </p>
                  <% } else { %>
                    <p class="muted" style="text-align:center;font-size:0.85rem;margin-top:5px;">
                      👤 Владелец: ID <%= p.owner %>
                    </p>
                  <% } %>
                <% } else { %>
                  <p class="muted" style="text-align:center;font-size:0.85rem;margin-top:5px;color:#f44336;">
                    ⚠️ Владелец не указан (создано админом)
                  </p>
                <% } %>

                <!-- Категория -->
                <% if (categories && p.category) { %>
                  <p class="muted" style="text-align:center;font-size:0.85rem;margin-top:5px;">
                    📁 <%= categories[p.category] || p.category %>
                  </p>
                <% } %>

                <div class="product-actions" style="flex-direction:column;gap:6px;margin-top:10px;">
                  <!-- Блокировка/Разблокировка -->
                  <button class="btn small toggle-visibility-btn" 
                          data-id="<%= p._id %>" 
                          data-status="<%= p.status %>"
                          style="<%= p.status === 'approved' ? 'background:#f44336;color:#fff;' : (p.status === 'rejected' ? 'background:#4caf50;color:#fff;' : 'background:#4caf50;color:#fff;') %>">
                    <%= p.status === 'approved' ? '🚫 Заблокировать' : '✅ Разблокировать' %>
                  </button>
                  
                  <a class="btn small outline" href="/admin/product/<%= p._id %>/edit">✏️ Редактировать</a>
                  
                  <form method="POST" action="/admin/product/<%= p._id %>/delete" style="display:inline" onsubmit="return confirm('Удалить карточку?');">
                    <button type="submit" class="btn small outline" style="width:100%;">🗑️ Удалить</button>
                  </form>
                  
                  <% if (p.link) { %>
                    <a class="btn small outline" href="<%= p.link %>" target="_blank" rel="noopener">🔗 Ссылка</a>
                  <% } %>
                  <% if (p.video_url) { %>
                    <button class="btn small outline" data-video="<%= p.video_url %>">▶️ Видео</button>
                  <% } %>
                </div>

                <!-- Рейтинг -->
                <div class="product-rating" data-id="<%= p._id %>" style="margin-top:10px;">
                  <button class="btn icon" type="button" disabled aria-label="Лайк">👍</button>
                  <span class="result"><%= (p.likes || 0) - (p.dislikes || 0) %></span>
                  <button class="btn icon" type="button" disabled aria-label="Дизлайк">👎</button>
                  <span class="votes">(<%= (p.likes || 0) + (p.dislikes || 0) %> голосов)</span>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="empty-state">Пока нет товаров</p>
      <% } %>
    </section>
  </main>

  <footer class="footer">
    <p>© 2025 EXTO Ecosystem</p>
  </footer>

  <!-- Модальное окно для отклонения -->
  <div id="rejectModal" class="modal" aria-hidden="true">
    <div class="modal-content" role="dialog" aria-modal="true" aria-label="Отклонить карточку">
      <button class="close" data-close-reject aria-label="Закрыть">&times;</button>
      <h3>Отклонить карточку</h3>
      <form id="rejectForm">
        <textarea name="reason" placeholder="Причина отклонения (необязательно)" rows="4" style="width:100%;padding:8px;border-radius:6px;border:1px solid #444;background:#2b2b2b;color:#fff;resize:vertical;"></textarea>
        <button type="submit" class="btn" style="background:#f44336;color:#fff;margin-top:10px;">Отклонить</button>
      </form>
    </div>
  </div>

  <!-- Модальное окно для видео -->
  <div id="videoModal" class="modal" aria-hidden="true">
    <div class="modal-content" role="dialog" aria-modal="true" aria-label="Видео обзор">
      <button class="close" data-close-video aria-label="Закрыть видео">&times;</button>
      <div id="videoFrame"></div>
    </div>
  </div>

  <script src="https://www.youtube.com/iframe_api"></script>

  <script>
    // Глобальная функция для YouTube API
    if (typeof window.onYouTubeIframeAPIReady === 'undefined') {
      window.onYouTubeIframeAPIReady = function() {
        console.log("✅ YouTube IFrame API готов (admin)");
        
        // Создаем плеер сразу при загрузке API
        const videoFrame = document.getElementById("videoFrame");
        if (videoFrame) {
          try {
            window.playerAdmin = new YT.Player('videoFrame', {
              width: '100%',
              height: '480',
              playerVars: {
                rel: 0,
                playsinline: 1
              },
              events: {
                'onReady': function(event) {
                  console.log("✅ Плеер готов (admin)");
                },
                'onError': function(event) {
                  console.error("❌ Ошибка плеера:", event.data);
                  alert("Ошибка при загрузке видео.");
                }
              }
            });
            console.log("✅ Плеер создан в onYouTubeIframeAPIReady (admin)");
          } catch (e) {
            console.error("❌ Ошибка создания плеера в onYouTubeIframeAPIReady:", e);
          }
        }
      };
    }
  </script>

  <script>

    // Функция извлечения videoId из YouTube URL
    function extractVideoId(url) {
      try {
        if (!url || typeof url !== 'string') return null;
        url = url.trim();
        
        if (url.includes('/embed/')) {
          const embedId = url.match(/embed\/([^?&#]+)/)?.[1];
          if (embedId) {
            return embedId.split('&')[0].split('#')[0].trim();
          }
        }
        
        let u;
        try {
          u = new URL(url);
        } catch (err) {
          if (!url.startsWith('http://') && !url.startsWith('https://')) {
            u = new URL('https://' + url);
          } else {
            throw err;
          }
        }
        
        const host = u.hostname.replace(/^www\./, "").toLowerCase();
        let videoId = null;
        
        if (host.includes("youtube.com")) {
          if (u.pathname === "/watch") {
            videoId = u.searchParams.get("v");
          } else if (u.pathname.startsWith("/embed/")) {
            videoId = u.pathname.split("/embed/")[1]?.split("?")[0];
          } else if (u.pathname.startsWith("/shorts/")) {
            videoId = u.pathname.split("/shorts/")[1]?.split("?")[0];
          } else if (u.pathname.startsWith("/v/")) {
            videoId = u.pathname.split("/v/")[1]?.split("?")[0];
          }
        } else if (host === "youtu.be") {
          videoId = u.pathname.slice(1).split("?")[0];
        }
        
        if (videoId) {
          videoId = videoId.split('&')[0].split('#')[0].trim();
          return videoId || null;
        }
        
        return null;
      } catch (err) {
        console.error("Ошибка извлечения videoId:", err);
        return null;
      }
    }

    document.addEventListener("DOMContentLoaded", () => {
      console.log("🔧 Инициализация админ-панели");
      
      // Блокировка/Разблокировка карточек
      const toggleBtns = document.querySelectorAll(".toggle-visibility-btn");
      console.log(`🔘 Найдено кнопок блокировки: ${toggleBtns.length}`);
      toggleBtns.forEach(btn => {
        btn.addEventListener("click", async (e) => {
          e.preventDefault();
          e.stopPropagation();
          console.log("🔘 Клик по кнопке блокировки");
          const id = btn.getAttribute("data-id");
          const currentStatus = btn.getAttribute("data-status");
          const action = currentStatus === "approved" ? "заблокировать" : "разблокировать";
          
          if (!confirm(`Вы уверены, что хотите ${action} эту карточку?`)) return;
          
          try {
            console.log(`📤 Отправка запроса на блокировку: /admin/product/${id}/toggle-visibility`);
            const res = await fetch(`/admin/product/${id}/toggle-visibility`, { method: "POST" });
            const data = await res.json();
            console.log("📥 Ответ сервера:", data);
            if (data.success) {
              alert(data.message || "Статус карточки изменен");
              location.reload();
            } else {
              alert("Ошибка: " + (data.message || "Неизвестная ошибка"));
            }
          } catch (err) {
            console.error("❌ Ошибка блокировки:", err);
            alert("Ошибка сети: " + err.message);
          }
        });
      });

      // Модерация: одобрение
      const approveBtns = document.querySelectorAll(".approve-btn");
      console.log(`✅ Найдено кнопок одобрения: ${approveBtns.length}`);
      approveBtns.forEach(btn => {
        btn.addEventListener("click", async (e) => {
          e.preventDefault();
          e.stopPropagation();
          console.log("✅ Клик по кнопке одобрения");
          const id = btn.getAttribute("data-id");
          console.log(`📤 ID карточки: ${id}`);
          if (!confirm("Одобрить эту карточку?")) return;
          try {
            console.log(`📤 Отправка запроса на одобрение: /admin/product/${id}/approve`);
            const res = await fetch(`/admin/product/${id}/approve`, { method: "POST" });
            const data = await res.json();
            console.log("📥 Ответ сервера:", data);
            if (data.success) {
              alert("Карточка одобрена!");
              location.reload();
            } else {
              alert("Ошибка: " + (data.message || "Неизвестная ошибка"));
            }
          } catch (err) {
            console.error("❌ Ошибка одобрения:", err);
            alert("Ошибка сети: " + err.message);
          }
        });
      });

      // Модерация: отклонение
      const rejectModal = document.getElementById("rejectModal");
      const rejectForm = document.getElementById("rejectForm");
      let currentRejectId = null;

      document.querySelectorAll(".reject-btn").forEach(btn => {
        btn.addEventListener("click", () => {
          currentRejectId = btn.getAttribute("data-id");
          rejectModal.style.display = "block";
          rejectModal.setAttribute("aria-hidden", "false");
        });
      });

      document.querySelector("[data-close-reject]")?.addEventListener("click", () => {
        rejectModal.style.display = "none";
        rejectModal.setAttribute("aria-hidden", "true");
        rejectForm.reset();
        currentRejectId = null;
      });

      rejectForm?.addEventListener("submit", async (e) => {
        e.preventDefault();
        if (!currentRejectId) return;
        const reason = new FormData(rejectForm).get("reason") || "Несоответствие правилам публикации";
        try {
          const res = await fetch(`/admin/product/${currentRejectId}/reject`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ reason })
          });
          const data = await res.json();
          if (data.success) {
            alert("Карточка отклонена");
            location.reload();
          } else {
            alert("Ошибка: " + (data.message || "Неизвестная ошибка"));
          }
        } catch (err) {
          console.error("Ошибка отклонения:", err);
          alert("Ошибка сети");
        }
      });

      // Видео модалка
      const videoModal = document.getElementById("videoModal");
      const videoFrame = document.getElementById("videoFrame");
      console.log(`🎥 Модальное окно видео:`, videoModal ? "найдено" : "не найдено");
      console.log(`🎥 iframe видео:`, videoFrame ? "найден" : "не найден");

      // Флаг для предотвращения закрытия сразу после открытия
      let isOpeningVideo = false;
      let lastVideoButtonClick = null; // Храним информацию о последнем клике на кнопку

      // Используем делегирование событий с capture phase для приоритета
      document.addEventListener("click", (e) => {
        const btn = e.target.closest("[data-video]");
        if (!btn) return;
        
        e.preventDefault();
        e.stopPropagation();
        e.stopImmediatePropagation(); // Останавливаем все последующие обработчики
        
        // Сохраняем информацию о клике
        lastVideoButtonClick = {
          time: Date.now(),
          target: e.target,
          button: btn
        };
        
        console.log("🖱️ Клик по кнопке видео:", btn);
        const url = btn.getAttribute("data-video");
        console.log("📹 URL из атрибута:", url);
        
        if (!url) {
          console.warn("⚠️ Кнопка с data-video не содержит URL");
          return;
        }
        
        const videoId = extractVideoId(url);
        console.log("🎬 Video ID:", videoId);
        
        if (videoId && videoModal && videoFrame) {
          // Устанавливаем флаг открытия ДО открытия модального окна
          isOpeningVideo = true;
          
          // Останавливаем предыдущий плеер
          if (playerAdmin) {
            try {
              playerAdmin.destroy();
              playerAdmin = null;
            } catch (e) {
              console.warn("Ошибка при уничтожении предыдущего плеера:", e);
            }
          }

          // Открываем модальное окно
          videoModal.style.display = "block";
          videoModal.setAttribute("aria-hidden", "false");
          console.log("✅ Модальное окно открыто");

          // Загружаем видео в существующий плеер
          if (window.playerAdmin && typeof window.playerAdmin.loadVideoById === 'function') {
            try {
              window.playerAdmin.loadVideoById(videoId);
              console.log("✅ Видео загружено в плеер (admin):", videoId);
            } catch (e) {
              console.error("❌ Ошибка загрузки видео:", e);
              alert("Ошибка загрузки видео. Пожалуйста, попробуйте еще раз.");
              closeVideoModal();
            }
          } else {
            console.error("❌ Плеер не готов или не найден (admin)");
            alert("Ошибка: видеоплеер не готов. Пожалуйста, обновите страницу.");
            closeVideoModal();
          }
          
          // Сбрасываем флаг через большую задержку
          setTimeout(() => {
            isOpeningVideo = false;
            if (videoModal.style.display === "block") {
              console.log("✅ Модальное окно успешно открыто и готово к использованию");
              setupCloseHandlers();
            }
          }, 800);
        } else {
          console.error("❌ Не удалось извлечь videoId из URL:", url);
          alert("Некорректная ссылка на видео. Поддерживаются только ссылки YouTube.");
          isOpeningVideo = false;
        }
      }, true); // Capture phase для приоритета

      // Функция закрытия модального окна
      function closeVideoModal() {
        // Не закрываем, если модальное окно только что открылось
        if (isOpeningVideo) {
          console.log("⏸️ Пропуск закрытия - модальное окно только что открылось");
          return;
        }
        
        if (lastVideoButtonClick && Date.now() - lastVideoButtonClick.time < 1000) {
          console.log("⏸️ Пропуск закрытия - недавний клик на кнопке видео");
          return;
        }
        
        console.log("❌ Закрытие модального окна видео");
        if (window.playerAdmin && typeof window.playerAdmin.stopVideo === 'function') {
          try {
            window.playerAdmin.stopVideo();
            console.log("✅ Видео остановлено (admin)");
          } catch (e) {
            console.warn("Ошибка при остановке видео:", e);
          }
        }
        if (videoModal) {
          videoModal.style.display = "none";
          setTimeout(() => {
            if (videoModal.style.display === "none") {
              videoModal.setAttribute("aria-hidden", "true");
            }
          }, 100);
        }
      }

      // Обработчик кнопки закрытия - устанавливаем после загрузки DOM
      function setupCloseHandlers() {
        const closeBtn = document.querySelector("[data-close-video]");
        if (closeBtn) {
          // Удаляем старый обработчик, если есть
          closeBtn.replaceWith(closeBtn.cloneNode(true));
          const newCloseBtn = document.querySelector("[data-close-video]");
          
          newCloseBtn.addEventListener("click", (e) => {
            e.preventDefault();
            e.stopPropagation();
            e.stopImmediatePropagation(); // Останавливаем все обработчики
            closeVideoModal();
          });
        }
      }
      
      // Устанавливаем обработчики после загрузки
      setupCloseHandlers();

      // Обработчик клика вне модального окна (только на overlay)
      // Используем обычную фазу, чтобы не конфликтовать с обработчиком открытия
      window.addEventListener("click", (e) => {
        // Пропускаем закрытие, если модальное окно только что открылось
        if (isOpeningVideo) {
          console.log("⏸️ Пропуск закрытия - модальное окно только что открылось");
          return;
        }
        
        // Пропускаем, если это был клик на кнопке видео (проверяем по времени и цели)
        if (lastVideoButtonClick && 
            Date.now() - lastVideoButtonClick.time < 1000 && 
            (e.target === lastVideoButtonClick.target || e.target === lastVideoButtonClick.button || e.target.closest("[data-video]"))) {
          console.log("⏸️ Пропуск закрытия - это клик на кнопке видео");
          return;
        }
        
        // Пропускаем, если клик был на кнопке видео или внутри модального окна
        if (e.target.closest("[data-video]") || e.target.closest(".modal-content")) {
          return;
        }
        
        // Проверяем, что:
        // 1. Клик именно на overlay (сам modal), а не на его содержимое
        // 2. Модальное окно открыто
        // 3. Клик не на кнопке закрытия
        if (e.target === videoModal && 
            videoModal.style.display === "block" &&
            !e.target.closest("[data-close-video]")) {
          console.log("❌ Закрытие модального окна по клику вне его");
          closeVideoModal();
        }
      }, false); // Обычная фаза, чтобы не конфликтовать

      // Закрытие по Escape
      window.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && videoModal && videoModal.style.display === "block") {
          closeVideoModal();
        }
      });
    });
  </script>

  <style>
    .modal {
      display: none;
      position: fixed;
      z-index: 2000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0,0,0,0.8);
    }
    .modal-content {
      background-color: #1a1a1a;
      margin: 5% auto;
      padding: 20px;
      border: 1px solid #444;
      border-radius: 12px;
      width: 90%;
      max-width: 600px;
      position: relative;
    }
    .close {
      color: #aaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
      background: none;
      border: none;
      position: absolute;
      right: 15px;
      top: 10px;
    }
    .close:hover {
      color: #fff;
    }
  </style>
</body>
</html>
