<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>EXTO — Каталог</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" href="/favicon.png" type="image/png">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="main-header">
    <div class="logo">EXTO</div>
    <nav class="nav">
      <ul>
        <li><a href="/">Каталог</a></li>
        <li><a href="#about">О проекте</a></li>
        <li><a href="#contacts">Контакты</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <!-- Основная рамка: каталог товаров -->
    <section id="catalog" class="section">
      <h2>Каталог товаров</h2>

      <div id="product-grid" class="product-grid">
        <% if (products && products.length) { %>
          <% products.forEach(p => { %>
            <div class="product-card">
              <!-- Название сверху -->
              <h3 class="product-title"><%= p.name %></h3>

              <!-- Картинка под названием -->
              <% if (p.image_url) { %>
                <img src="<%= p.image_url %>" alt="<%= p.name %>">
              <% } else { %>
                <div class="no-image">Нет изображения</div>
              <% } %>

              <!-- Нижний блок: описание + цена + кнопки -->
              <div class="product-bottom">
                <p class="product-desc"><%= p.description %></p>
                <span class="price"><%= p.price %> ₸</span>
                <div class="product-actions">
                  <a class="product-action" href="<%= p.link %>" target="_blank" rel="noopener">Перейти</a>

                  <!-- ✅ Кнопка "Обзор" только если video_url задан -->
                  <% if (p.video_url) { %>
                    <button class="product-review" data-video="<%= p.video_url %>">Обзор</button>
                  <% } %>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p class="empty-state">Пока нет товаров</p>
        <% } %>
      </div>
    </section>

    <!-- Блок "О проекте" -->
    <section id="about" class="section">
      <h2>О проекте</h2>
      <p class="disclaimer">
        EXTO — это каталог рекомендаций. Мы стремимся к привычному UX, чистой визуальной и стабильной верстке.
      </p>
    </section>

    <!-- Блок "Контакты" -->
    <section id="contacts" class="section">
      <h2>Контакты</h2>
      <p class="disclaimer">
        По вопросам сотрудничества и предложений — пишите нам.
      </p>
    </section>
  </main>

  <footer class="footer">
    <p>© 2025 EXTO Ecosystem</p>
  </footer>

  <!-- Модальное окно для видео -->
  <div id="videoModal" class="modal" aria-hidden="true">
    <div class="modal-content" role="dialog" aria-modal="true" aria-label="Видео обзор">
      <button class="close" aria-label="Закрыть видео">&times;</button>
      <iframe id="videoFrame" width="100%" height="480" frameborder="0" allowfullscreen></iframe>
    </div>
  </div>

  <script src="/script.js"></script>
</body>
</html>
