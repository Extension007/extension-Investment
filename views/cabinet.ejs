<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Личный кабинет</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="main-header">
    <div class="container header-inner">
      <a class="logo" href="/">EXTO</a>
      <nav class="nav">
        <ul class="nav-list">
          <li><a class="nav-link" href="/">Каталог</a></li>
        </ul>
      </nav>
      <div class="header-actions">
        <span class="user-chip">👤 <%= user.username %></span>
      </div>
    </div>
  </header>

  <main class="container">
    <section class="section">
      <h2>Создать карточку товара</h2>
      <form id="createProductForm" class="form-grid" enctype="multipart/form-data" method="POST" action="/cabinet/product">
        <input type="text" name="name" placeholder="Название" required>
        <input type="text" name="description" placeholder="Описание">
        <input type="url" name="link" placeholder="Ссылка">
        <input type="url" name="video_url" placeholder="YouTube ссылка">
        <input type="number" name="price" placeholder="Цена" min="0" step="1" required>
        <input type="file" name="image" accept="image/*">
        <select name="category" required>
          <option value="home">Для дома</option>
          <option value="beauty">Красота и здоровье</option>
          <option value="auto">Авто мото</option>
          <option value="electric">Электрика</option>
          <option value="electronics">Электроника</option>
          <option value="plumbing">Сантехника</option>
        </select>
        <button type="submit" class="btn primary">Отправить</button>
      </form>
      <div id="createProductMsg" class="note" style="margin-top:10px;"></div>
    </section>

    <section class="section">
      <h2>Мои карточки</h2>
      <% if (products && products.length) { %>
        <div class="product-grid">
          <% products.forEach(p => { %>
            <div class="product-card">
              <h3 class="product-title"><%= p.name %></h3>
              <% if (p.image_url) { %>
                <img src="<%= p.image_url %>" alt="<%= p.name %>">
              <% } else { %>
                <div class="no-image">Нет изображения</div>
              <% } %>
              <div class="product-bottom">
                <div style="text-align:center;margin-bottom:10px;">
                  <% if (p.status === "pending") { %>
                    <span class="badge" style="background:#ff9800;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ⏳ На модерации
                    </span>
                  <% } else if (p.status === "approved") { %>
                    <span class="badge" style="background:#4caf50;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ✅ Опубликовано
                    </span>
                  <% } else if (p.status === "rejected") { %>
                    <span class="badge" style="background:#f44336;color:#fff;padding:4px 12px;border-radius:6px;font-size:0.9rem;">
                      ❌ Отклонено
                    </span>
                    <% if (p.rejection_reason) { %>
                      <p style="color:#f44336;font-size:0.85rem;margin-top:5px;"><%= p.rejection_reason %></p>
                    <% } %>
                  <% } %>
                </div>

                <div class="row">
                  <span class="muted">Текущая цена:</span>
                  <strong class="price" data-id="<%= p._id %>"><%= p.price %></strong>
                </div>
                <% if (p.status === "approved" || p.status === "pending") { %>
                  <form class="priceForm" data-id="<%= p._id %>">
                    <input type="number" name="price" min="0" step="1" placeholder="Новая цена" required>
                    <button type="submit" class="btn small">Обновить цену</button>
                  </form>
                <% } %>
                <p class="product-desc"><%= p.description || "—" %></p>
                <div class="product-actions">
                  <% if (p.link) { %>
                    <a class="btn small outline" href="<%= p.link %>" target="_blank" rel="noopener">Ссылка</a>
                  <% } %>
                  <% if (p.video_url) { %>
                    <button class="btn small" data-video="<%= p.video_url %>">Обзор</button>
                  <% } %>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="empty-state">У вас пока нет карточек.</p>
      <% } %>
    </section>
  </main>

<footer class="footer">
  <p>© 2025 EXTO Ecosystem</p>
</footer>

<!-- FIX: Полноэкранный overlay для YouTube видео -->
<div id="videoOverlay" class="video-overlay" aria-hidden="true" style="display:none;">
  <div class="video-overlay-container">
    <button class="video-overlay-close" data-close-video aria-label="Закрыть видео">&times;</button>
    <div class="video-overlay-iframe-container" id="videoIframeContainer">
      <!-- FIX: iframe создается динамически по клику -->
    </div>
  </div>
</div>

<script>
  // FIX: Извлечение videoId из разных форматов ссылок YouTube
  function extractVideoId(url) {
    if (!url) return null;
    if (url.includes('/embed/')) return url.split('/embed/')[1].split(/[?#]/)[0];
    if (url.includes('/shorts/')) return url.split('/shorts/')[1].split(/[?#]/)[0];
    if (url.includes('youtu.be/')) return url.split('youtu.be/')[1].split(/[?#]/)[0];
    const match = url.match(/[?&]v=([^&]+)/);
    return match ? match[1] : null;
  }

  // FIX: Формирование URL для YouTube embed с параметрами для всех устройств
  // Используем youtube-nocookie.com для лучшей совместимости и избежания ошибки 153
  function buildYouTubeEmbedUrl(videoId) {
    if (!videoId) return '';
    // FIX: Используем минимальные параметры, autoplay добавим через API
    // origin важен для безопасности YouTube API
    return `https://www.youtube-nocookie.com/embed/${videoId}?rel=0&modestbranding=1&playsinline=1&enablejsapi=1&controls=1&origin=${encodeURIComponent(window.location.origin)}`;
  }
  
  // FIX: Глобальная переменная для хранения YouTube плеера
  let youtubePlayer = null;

  document.addEventListener("DOMContentLoaded", () => {
    const form = document.getElementById("createProductForm");
    const msg = document.getElementById("createProductMsg");

    if (form) {
      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(form);
        try {
          const res = await fetch("/cabinet/product", { method: "POST", body: formData });
          const json = await res.json();
          if (json.success) {
            msg.textContent = "Карточка отправлена на модерацию.";
            msg.style.color = "green";
            setTimeout(() => location.reload(), 800);
          } else {
            msg.textContent = json.message || "Ошибка";
            msg.style.color = "#b00020";
          }
        } catch (err) {
          msg.textContent = "Ошибка сети: " + err.message;
          msg.style.color = "#b00020";
        }
      });
    }

    document.querySelectorAll(".priceForm").forEach((f) => {
      f.addEventListener("submit", async (e) => {
        e.preventDefault();
        const id = f.getAttribute("data-id");
        const price = f.querySelector("input[name='price']").value;
        try {
          const res = await fetch(`/cabinet/product/${id}/price`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ price })
          });
          const json = await res.json();
          if (json.success) {
            const priceEl = document.querySelector(`.price[data-id='${id}']`);
            if (priceEl) priceEl.textContent = json.price;
          } else {
            alert(json.message || "Ошибка обновления цены");
          }
        } catch (err) {
          alert("Ошибка сети: " + err.message);
        }
      });
    });

    // FIX: Полноэкранный overlay для YouTube видео
    const videoOverlay = document.getElementById('videoOverlay');
    const videoIframeContainer = document.getElementById('videoIframeContainer');
    
    // FIX: Функция открытия видео overlay (работает на всех устройствах)
    function openVideoOverlay(videoUrl) {
      const videoId = extractVideoId(videoUrl);
      if (!videoId) {
        console.warn('⚠️ Не удалось извлечь videoId из URL:', videoUrl);
        return;
      }
      
      // FIX: Очищаем предыдущий плеер
      if (youtubePlayer) {
        try {
          youtubePlayer.destroy();
        } catch (e) {
          // Игнорируем ошибки
        }
        youtubePlayer = null;
      }
      
      // FIX: Очищаем контейнер перед добавлением нового iframe
      videoIframeContainer.innerHTML = '';
      
      // FIX: Показываем overlay
      videoOverlay.style.display = 'flex';
      videoOverlay.setAttribute('aria-hidden', 'false');
      
      // FIX: Предотвращаем скролл body при открытом видео (важно для мобильных)
      document.body.style.overflow = 'hidden';
      
      // FIX: Загружаем YouTube IFrame API если еще не загружен
      if (typeof YT === 'undefined' || typeof YT.Player === 'undefined') {
        const tag = document.createElement('script');
        tag.src = 'https://www.youtube.com/iframe_api';
        const firstScriptTag = document.getElementsByTagName('script')[0];
        firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);
        
        // FIX: Ждем загрузки API
        window.onYouTubeIframeAPIReady = function() {
          createYouTubePlayer(videoId);
        };
      } else {
        // FIX: API уже загружен, создаем плеер сразу
        createYouTubePlayer(videoId);
      }
      
      console.log('▶️ Открытие видео:', videoId);
    }
    
    // FIX: Функция создания YouTube плеера с автовоспроизведением
    function createYouTubePlayer(videoId) {
      try {
        youtubePlayer = new YT.Player(videoIframeContainer, {
          videoId: videoId,
          playerVars: {
            'rel': 0,
            'modestbranding': 1,
            'playsinline': 1,
            'controls': 1,
            'autoplay': 1,
            'mute': 1,
            'enablejsapi': 1,
            'origin': window.location.origin
          },
          events: {
            'onReady': function(event) {
              // FIX: Запускаем видео после готовности плеера
              try {
                event.target.playVideo();
                // FIX: Включаем звук через небольшую задержку после начала воспроизведения
                setTimeout(function() {
                  try {
                    event.target.unMute();
                  } catch (e) {
                    // Игнорируем ошибки включения звука
                  }
                }, 1000);
              } catch (e) {
                console.log('ℹ️ Автовоспроизведение недоступно, пользователь может запустить вручную');
              }
            },
            'onError': function(event) {
              console.error('❌ Ошибка YouTube плеера:', event.data);
              if (event.data === 153) {
                console.log('ℹ️ Ошибка 153: Видео готово, но автовоспроизведение заблокировано. Нажмите play вручную.');
              }
            }
          }
        });
      } catch (e) {
        console.error('❌ Ошибка создания YouTube плеера:', e);
        // FIX: Fallback - используем обычный iframe если API не работает
        const iframe = document.createElement('iframe');
        iframe.src = buildYouTubeEmbedUrl(videoId);
        iframe.setAttribute('allow', 'autoplay; encrypted-media; fullscreen; picture-in-picture; accelerometer; gyroscope');
        iframe.setAttribute('playsinline', '1');
        iframe.setAttribute('frameborder', '0');
        iframe.setAttribute('allowfullscreen', '');
        iframe.setAttribute('webkitallowfullscreen', '');
        iframe.setAttribute('mozallowfullscreen', '');
        iframe.style.width = '100%';
        iframe.style.height = '100%';
        iframe.style.border = 'none';
        iframe.style.display = 'block';
        videoIframeContainer.innerHTML = '';
        videoIframeContainer.appendChild(iframe);
      }
    }
    
    // FIX: Функция закрытия видео overlay
    function closeVideoOverlay() {
      // FIX: Останавливаем и уничтожаем YouTube плеер
      if (youtubePlayer) {
        try {
          youtubePlayer.stopVideo();
          youtubePlayer.destroy();
        } catch (e) {
          // Игнорируем ошибки
        }
        youtubePlayer = null;
      }
      
      // FIX: Очищаем src у iframe для остановки воспроизведения (fallback)
      const iframe = videoIframeContainer.querySelector('iframe');
      if (iframe) {
        iframe.src = '';
      }
      
      // FIX: Очищаем контейнер
      videoIframeContainer.innerHTML = '';
      
      // FIX: Скрываем overlay
      videoOverlay.style.display = 'none';
      videoOverlay.setAttribute('aria-hidden', 'true');
      
      // FIX: Восстанавливаем скролл body
      document.body.style.overflow = '';
      
      console.log('🔒 Закрытие видео overlay');
    }
    
    // FIX: Обработчик клика и touch-событий на кнопку "Обзор" и закрытия overlay (работает на всех устройствах)
    const handleVideoClick = (e) => {
      // FIX: Открытие видео по клику на кнопку "Обзор"
      const videoBtn = e.target.closest('.btn[data-video]');
      if (videoBtn) {
        e.preventDefault();
        e.stopPropagation();
        const videoUrl = videoBtn.getAttribute('data-video');
        if (videoUrl) {
          openVideoOverlay(videoUrl);
        }
        return;
      }
      
      // FIX: Закрытие overlay по кнопке закрытия
      if (e.target.closest('[data-close-video]')) {
        e.preventDefault();
        e.stopPropagation();
        closeVideoOverlay();
        return;
      }
      
      // FIX: Закрытие overlay по клику на фон (но не на контейнер с видео)
      if (e.target === videoOverlay) {
        closeVideoOverlay();
        return;
      }
    };
    
    // FIX: Поддержка клика и touch для всех устройств
    document.addEventListener('click', handleVideoClick, true);
    document.addEventListener('touchend', handleVideoClick, true);
  });
</script>

</body>
</html>

