<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>О нас — Albamount</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes">
  <meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/header-tabs.css">
</head>
<body>
  <!-- Одноэтажный header без вкладок -->
  <header class="main-header">
    <div class="container header-inner">
      <!-- Основные элементы -->
      <div class="header-top">
        <div class="header-left">
          <a class="logo" href="/"><img src="/albamount.png" alt="ALBAMOUNT" class="logo-img"></a>
          <div class="header-stats">
            <div class="stat-item">
              <span style="opacity: 0.8;">👥</span>
              <strong style="color: #4caf50;"><%= typeof visitorCount !== 'undefined' ? visitorCount : 0 %></strong>
            </div>
            <div class="stat-item">
              <span style="opacity: 0.8;">👤</span>
              <strong style="color: #2196f3;"><%= typeof userCount !== 'undefined' ? userCount : 0 %></strong>
            </div>
          </div>
        </div>
        <div class="header-actions">
          <% if (isAuth) { %>
            <% if (isAdmin) { %>
              <a class="btn outline" href="/admin">Админ-панель</a>
            <% } else { %>
              <a class="btn outline" href="/cabinet">Личный кабинет</a>
            <% } %>
          <% } else { %>
            <a class="btn outline" href="/user/login">Вход</a>
            <button id="openRegister" type="button" class="btn outline">Регистрация</button>
          <% } %>
        </div>
      </div>
    </div>
  </header>

  <main>
    <!-- ОСНОВНАЯ СЕКЦИЯ О НАС -->
    <section id="about" class="section about-section">
      <div class="section-header">
        <h2>О проекте Albamount</h2>
        <p class="about-description">
          Мы создали современную платформу для поиска и рекомендаций товаров и услуг, 
          которая объединяет продавцов и покупателей в удобной экосистеме.
        </p>
      </div>

      <div class="about-content">
        <!-- О компании -->
        <div class="about-main">
          <h3>🎯 Наша миссия</h3>
          <div class="mission-content">
            <p>
              Albamount — это инновационная платформа, которая стремится стать мостом между качественными
              товарами, услугами и их потребителями. Мы верим, что каждый заслуживает доступа к 
              лучшим решениям для своих потребностей.
            </p>
            <p>
              Наша цель — создать прозрачную, удобную и надежную экосистему, где каждый может 
              найти именно то, что ищет, а продавцы — показать свои лучшие предложения.
            </p>
          </div>

          <h3>⚡ Что нас отличает</h3>
          <div class="features-grid">
            <div class="feature-card">
              <div class="feature-icon">🎨</div>
              <h4>Привычный UX</h4>
              <p>Интуитивный интерфейс, к которому легко привыкнуть с первого клика</p>
            </div>
            
            <div class="feature-card">
              <div class="feature-icon">🎭</div>
              <h4>Чистая визуализация</h4>
              <p>Минималистичный дизайн без лишних элементов, фокус на контенте</p>
            </div>
            
            <div class="feature-card">
              <div class="feature-icon">🛡️</div>
              <h4>Стабильная верстка</h4>
              <p>Адаптивность для всех устройств и браузеров без компромиссов</p>
            </div>
            
            <div class="feature-card">
              <div class="feature-icon">⚡</div>
              <h4>Высокая скорость</h4>
              <p>Оптимизированная загрузка и мгновенная отзывчивость интерфейса</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer с вкладками для мобильных устройств -->
  <footer class="mobile-tabs-footer">
    <div class="mobile-tabs-container">
      <a href="/products" class="mobile-tab-button <%= '' %>">
        Товары
      </a>
      <a href="/services" class="mobile-tab-button <%= '' %>">
        Услуги
      </a>
      <a href="/ad" class="mobile-tab-button <%= '' %>">
        Реклама
      </a>
      <a href="/about" class="mobile-tab-button active">
        О Нас
      </a>
      <a href="/contacts" class="mobile-tab-button <%= '' %>">
        Контакты
      </a>
    </div>
  </footer>

<!-- Модальное окно регистрации -->
<div id="registerModal" class="modal" aria-hidden="true" style="display:none;">
  <div class="modal-content" role="dialog" aria-modal="true" aria-label="Регистрация">
    <button class="close" data-close-register aria-label="Закрыть">&times;</button>
    <h3>Регистрация</h3>
    <form id="registerForm">
      <input type="text" name="username" placeholder="Никнейм" required>
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Пароль" minlength="6" required>
      <button type="submit">Создать аккаунт</button>
    </form>
    <div id="registerError" class="error" style="color:#b00020;margin-top:10px;display:none;"></div>
    <div id="registerSuccess" style="color:green;margin-top:10px;display:none;"></div>
  </div>
</div>

<% let isAuthValue = typeof isAuth !== 'undefined' ? isAuth : false; %>
<% if (isAuthValue) { %>
<script>window.IS_AUTH = true;</script>
<% } else { %>
<script>window.IS_AUTH = false;</script>
<% } %>

<% let isAdminValue = typeof isAdmin !== 'undefined' ? isAdmin : false; %>
<% if (isAdminValue) { %>
<script>window.IS_ADMIN = true;</script>
<% } else { %>
<script>window.IS_ADMIN = false;</script>
<% } %>

<% let userRoleValue = typeof userRole !== 'undefined' ? userRole : null; %>
<script>window.USER_ROLE = '<%= userRoleValue %>';</script>

<script src="/script.js"></script>

<!-- Подключаем скрипт для предотвращения перекрытия контента футером -->
<script src="/fix-footer-overlap.js"></script>

<style>
/* Дополнительные стили для страницы О нас */
.about-section {
  background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
}

.about-description {
  text-align: center;
  color: #ccc;
  font-size: 1.2rem;
  margin-bottom: 40px;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
  line-height: 1.6;
}

.about-content {
  display: grid;
  gap: 40px;
}

.about-main h3 {
  color: #ff9999;
  margin-bottom: 20px;
  font-size: 1.4rem;
  text-align: center;
}

.mission-content {
  background: rgba(255, 51, 51, 0.05);
  border: 1px solid rgba(255, 51, 51, 0.2);
  border-radius: 12px;
  padding: 25px;
  margin-bottom: 30px;
}

.mission-content p {
  color: #ddd;
  line-height: 1.6;
  margin-bottom: 15px;
}

.mission-content p:last-child {
  margin-bottom: 0;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-bottom: 30px;
}

.feature-card {
  background: rgba(255, 51, 51, 0.05);
  border: 1px solid rgba(255, 51, 51, 0.2);
  border-radius: 12px;
  padding: 20px;
  text-align: center;
  transition: all 0.3s ease;
}

.feature-card:hover {
  background: rgba(255, 51, 51, 0.1);
  transform: translateY(-2px);
}

.feature-icon {
  font-size: 2.5rem;
  margin-bottom: 15px;
  display: block;
}

.feature-card h4 {
  color: #ff3333;
  margin-bottom: 10px;
  font-size: 1.1rem;
}

.feature-card p {
  color: #ccc;
  font-size: 0.95rem;
  line-height: 1.5;
}

/* Адаптивность */
@media (max-width: 768px) {
  .about-description {
    font-size: 1.1rem;
    margin-bottom: 30px;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
    gap: 15px;
  }
}
</style>
</body>
</html>
